export class Product{
    id: string;
    name: string;
    desc: string;
    price: number;

    constructor(id, name, desc, price){

        this.id = id;

        this.name = name;

        this.desc = desc;

        this.price = price;

    }

}